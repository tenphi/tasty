---
'@tenphi/tasty': patch
---

Fix complementary factoring for compound state conditions (e.g. `@dark-root`, `@high-contrast-root`), eliminating duplicate and overlapping CSS selectors when token values match across state combinations.
