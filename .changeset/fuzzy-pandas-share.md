---
'@tenphi/tasty': minor
---

Add a Next.js config wrapper that extracts eager configured globals into one
content-hashed, immutable shared stylesheet while route-specific CSS keeps
using the streaming App Router registry.
