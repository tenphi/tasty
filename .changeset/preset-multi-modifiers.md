---
'@tenphi/tasty': minor
---

Support multiple space-separated modifiers in the `preset` style. Modifiers can now be combined after the slash (e.g. `preset="h2 / strong italic"`, `preset="t2 / strong tight"`) and in the modifier-only shorthand (e.g. `preset="bold italic"`). Existing single-modifier syntax is unchanged.
