---
'@tenphi/tasty': patch
---

Change default `letterSpacing` in typography presets from `'0'` to `'normal'`. The previous default could override inherited letter-spacing; `'normal'` matches the browser default.
