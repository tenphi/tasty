import { tasty } from '@tenphi/tasty';

export const Shared = tasty({
  styles: {
    display: 'block',
    padding: '10px',
  },
});

export const PageOnly = tasty({
  styles: {
    margin: '12px',
  },
});
